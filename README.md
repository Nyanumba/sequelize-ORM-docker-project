# sequelize-ORM-docker-project
This project is a backend API for managing customers, orders, and products built using Express, Sequelize ORM, and PostgreSQL. It supports full CRUD operations with one-to-many and many-to-many associations. The application is fully containerized using Docker and uses migrations and seeders for database management.
